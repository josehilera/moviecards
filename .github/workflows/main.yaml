name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: construccion
        run: echo "construcción"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: pruebas
        run: echo "pruebas"
  
  qa:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: calidad
        run: echo "calidad"

  deploy:
    needs: qa
    runs-on: ubuntu-latest
    steps:
      - name: despliegue
        run: echo "despliegue"
